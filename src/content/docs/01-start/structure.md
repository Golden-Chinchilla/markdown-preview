# 目录结构

左侧目录来自 Markdown 文件的相对路径。

## 示例

如果文件放在：

```txt
src/content/docs/work/meeting/weekly.md
```

网站里会显示为：

```txt
work
  meeting
    weekly.md
```

## 排序

文件和文件夹会按照名称排序。你可以用数字前缀控制顺序，例如：

- `01-start`
- `02-guides`
- `03-reference`
